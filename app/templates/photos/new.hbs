<div>
  {{input id='createImage' value=newImage placeholder="Image url"}}
  {{input id='createName' value=newName placeholder="Name"}}
  {{textarea id='createDescription' value=newDescription placeholder="Description"}}
</div>
<button {{action 'createPhoto'}}>Save Photo</button>
